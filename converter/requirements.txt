# Shared with the main app (app/db.py, app/config.py, app/ollama.py, ...)
aiosqlite>=0.20,<1.0
arq>=0.26,<1.0
httpx>=0.27,<1.0
pydantic>=2.8,<3.0
pydantic-settings>=2.4,<3.0
redis>=5.0,<6.0

# HF repo download + HF -> GGUF conversion
# (mirrors llama.cpp's requirements/requirements-convert_hf_to_gguf.txt at
# the pinned LLAMACPP_REF - keep these two in sync when bumping the ref)
huggingface_hub>=0.25,<1.0
numpy~=1.26.4
sentencepiece>=0.1.98,<0.3.0
transformers==4.57.6
gguf>=0.1.0
protobuf>=4.21.0,<5.0.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0
